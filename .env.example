# Enable if connecting to a self-signed cert
# NODE_TLS_REJECT_UNAUTHORIZED=0

# The headless frontend domain URL. Uncomment this line and ensure the value matches the URL used by your frontend app.
# NEXT_PUBLIC_NEXT_APP_URL=http://localhost:3000

# The WordPress "frontend" domain URL
NEXT_PUBLIC_WORDPRESS_SITE_URL=https://headless-demo.local

# The WordPress "frontend" domain URL
NEXT_PUBLIC_WORDPRESS_HOME_URL=https://headless-demo.local

# The WordPress GraphQL endpoint
NEXT_PUBLIC_WORDPRESS_GRAPHQL_ENDPOINT=graphql

# The WordPress Uploads directory path
# NEXT_PUBLIC_WORDPRESS_UPLOADS_PATH=/wp-content/uploads

# The WordPress REST URL Prefix
# NEXT_PUBLIC_WORDPRESS_REST_URL_PREFIX=/wp-json

# Token used for authenticating GraphQL introspection queries
INTROSPECTION_TOKEN=

# Enable if you want to bypass CORS restrictions from WordPress server note: this feature is to bypass CORS restrictions for script modules. Default: process.env.NODE_ENV === 'development'
# NEXT_PUBLIC_USE_CORS_PROXY=false

# The CORS proxy prefix to use when bypassing CORS restrictions from WordPress server, Default: /proxy, This means for script module next app will make request NEXT_PUBLIC_NEXT_APP_URL/proxy/{module-path}
# NEXT_PUBLIC_CORS_PROXY_PREFIX=/proxy
