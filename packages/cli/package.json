{
	"name": "snapwp",
	"version": "0.0.4",
	"license": "AGPL-3.0",
	"author": "rtCamp",
	"description": "A better way to build headless WordPress applications.",
	"type": "module",
	"files": [
		"./dist"
	],
	"bin": {
		"snapwp": "./dist/snapwp.cjs"
	},
	"scripts": {
		"build": "node ./build.cjs",
		"force-build": "rm -rf dist && npm run build"
	},
	"dependencies": {
		"commander": "^13.1.0"
	}
}
